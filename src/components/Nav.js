import React from 'react'
import classes from './Nav.module.css'

const Nav = () => {
  return (
    <div className={classes.NavBar}>
      <h1>NavBar</h1>
      <button>Login</button>
    </div>
  )
}

export default Nav
